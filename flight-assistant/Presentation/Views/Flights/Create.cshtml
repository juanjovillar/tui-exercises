@model Presentation.Flights.Models.FlightViewModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

@{
    ViewBag.Title = "Create Sale";
}

<h2>Create Flight</h2>

@using (Html.BeginForm("create", "flights", FormMethod.Post))
{
<form class="form-horizontal">
    <div class="form-group">
        <label class="control-label">Source Airport</label>
        @*@Html.DropDownListFor(p => p.Airtports.AirportId, Model.Airports, new { @class = "form-control" })*@
    </div>
    <div class="form-group">
        <label class="control-label">Destination Airport</label>
        @*@Html.DropDownListFor(p => p.Airtports.AirportId, Model.Airports, new { @class = "form-control" })*@
    </div>
    <div class="form-group">
        <label class="control-label">Aircraft</label>
        @*@Html.DropDownListFor(p => p.Airtports.AircraftId, Model.Aircrafts, new { @class = "form-control" })*@
    </div>
    <div class="form-group">
        <label class="control-label">Flight Name</label>
        @*@Html.TextBoxFor(p => p.Flight.IATA, new { @class = "form-control" })*@
    </div>
    <button type="submit" class="btn btn-primary">Create</button>
</form>
}

